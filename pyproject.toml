[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "efficient_tutor_backend"
version = "1.0.0"
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = [
    "tests",
]
